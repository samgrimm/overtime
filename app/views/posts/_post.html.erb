<tr>
  <td>
    <%= post.overtime_request %>
  </td>
  <td>
    <%= post.date %>
  </td>
  <td class="hidden-xs-down">
    <%= link_to truncate(post.rationale), post %>
  </td>

    <%= status_label post.status %>

  <td>
    <%= link_to '', edit_post_path(post), id:"edit_#{post.id}", class:"fa fa-pencil index-icons" if policy(post).update?  %>

  </td>
  <td>
    <%= link_to '', post_path(post), method: :delete, id:"delete_#{post.id}", class:"fa fa-trash index-icons", data: { confirm: "Are you sure?"}%>
  </td>
</tr>
